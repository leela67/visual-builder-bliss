# MongoDB Configuration (VITE_ prefix for frontend access)
VITE_MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.example.mongodb.net/your-database-name

# Application Configuration
NODE_ENV=development
PORT=3000

# Legacy support for scripts (non-VITE prefixed)
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.example.mongodb.net/your-database-name

# JWT Secret (for authentication - if implemented later)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# Email Configuration (for notifications - if implemented later)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password